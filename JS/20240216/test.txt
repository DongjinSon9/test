@media(화면크기){}      = 미디어쿼리 명령어 
모바일 , 태블릿, 랩탑, 워치, 기종별 사이즈 => https://yesviz.com/devices.php
자바 스크립트 ; 프로그래밍 언어
1. 변수
2. 연산자
3. 조건문(if), 반복문(for)
4. 클래스
5. 함수
6. 내장 클래스 = 내부 기능

프로그래밍 언어 배우는 순서(어려운 순서) : 자바(보수적) > 자바스크립트 >> 파이썬
예) 자바 코드 길이 10이라면 자바스크립트는 6줄 파이썬은 4줄이 됨.

//document.querySelector('body').style.backgroundColor = 'black';
 객체 : 수납도구       . : 닷연산자. 하위로 연결하는 구분자로 사용한다. 
 예) 탐색기 : > 내pc > 바탕화면 > 폴더
      cmd  : \내pc\바탕화면\폴더

document.write('hello world'); /* document라는 폴더에 write라는 함수가 있고 hello wold를 출력한다. */

alert : alert() 소괄호 안의 내용을 작은 경고창에 표시해서 띄워주는 함수.

var 로 변수를 지정하고 각 변수에 값을 읽어들인다음 함수를 거쳐 출력한다.


//문자열 기능, 내장 기능, 기본 클래스 등 여러 표현이 있는데 그만큼 자주 사용되는 기능이다.
ex) 이미지 파일인지 어떻게 알 것인가?
abc.png, def.mp4   -> 마일명 : 문자열로 다 구성이 됨.   
0123456  0123456 
ㄴ!특정의 요소를 선택하기.  . 뒤의 값을 찾기.
.indexOf(".") => 4 가 출력 됨.




-----------------------------------------------------데이터 타입------------------------------------------- //매우 매우 중요.
https://developer.mozilla.org/ko/docs/Web/JavaScript/Data_structures

동적 타이핑
ex) var a ="abc" 변수를 입력.
타입스크립트 => 문자면 문자, 정수면 정수, 타입을 명시함.

자바 데이터 타입
ex) int(기본형) a = 3, string(참조형) str = "123"
동적 타이밍을 지원하기 시작.


null = 할당이 안 됐다.
undefined = 값이 없다.
